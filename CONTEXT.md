# Clash Verge Kit

Clash Verge Kit 帮助 Clash Verge Rev 用户把多个子订阅安全地编排进已有主配置，并生成可检查、可重复执行的嵌套脚本。当前产品基线是 [CLI-only V1 正式规格](docs/specs/2026-08-23-cli-only-v1.md)。

## 产品语言

**Clash Verge Kit**：
供单个用户在本机终端中使用的 Clash Verge Rev 工具箱；当前正式能力是嵌套脚本生成与安全写入。
_Avoid_: 本地 Web 工具、网页、桌面应用、在线版

**公开分发**：
当前通过 GitHub 源码仓库分发；npm 包尚未发布。首次公开发布完成后，普通用户才可通过 `npx clash-verge-kit` 启动。
_Avoid_: 把尚未发布的 npm 包描述为当前可用入口、本地部署、启动服务

**目标主订阅**：
用户希望嵌套脚本在 Clash Verge Rev 中匹配并执行的本机订阅身份，由显示名称和来源共同识别；它必须已经导入 Clash Verge Rev，不接受手工 URL 代替本机身份。它不是脚本执行时传入的完整配置对象。
_Avoid_: 主配置、主程序、服务器配置

**目标选择菜单**：
用户从本机已导入订阅中选择目标主订阅的第一层配置菜单；此时尚未形成可复用配置草稿，数字 `0` 直接退出 CLI，不返回语言选择，也不原地刷新同一菜单。
_Avoid_: 订阅管理器、刷新按钮、返回语言菜单

**运行配置**：
Clash Verge Rev 执行嵌套脚本时传入、并会被脚本检查和修改的实际配置对象。
_Avoid_: 主订阅文件、服务器配置

**主代理组**：
运行配置中承载原有出口、并接收嵌套子组的目标代理组。
_Avoid_: 主订阅、总配置

**子订阅**：
准备被加入目标主订阅运行配置的独立代理来源。
同一嵌套配置最多保留 64 个子订阅；来源经规范化后不得重复，达到上限后不能继续追加。
_Avoid_: 账号、代理服务

**嵌套配置**：
把一个或多个子订阅分别组织成独立代理组，再将这些代理组加入运行配置中的主代理组。
_Avoid_: 订阅合并、流量聚合

**嵌套脚本**：
描述一次嵌套配置的自包含、可人工检查的扩展脚本，可被用户交给 Clash Verge Rev 执行。
_Avoid_: 部署脚本、后端脚本、压缩产物

**生成器**：
把用户选择转换为嵌套脚本，并提供诊断、遮罩预览和管理信息的产品核心。
_Avoid_: 代理面板、代理客户端

**遮罩预览**：
用户明确请求后用于检查嵌套脚本的终端视图，其中订阅来源被遮罩；默认流程只显示生成摘要，完整脚本只在确认后进入剪贴板或脚本写入。
_Avoid_: 默认脚本展示、完整脚本回显、删除订阅来源

**订阅检查**：
根据用户明确选择的本机订阅或手工提交的子订阅公网 HTTPS 来源，直接读取名称、格式、可选代理组和节点数量的辅助过程；远程检查访问用户输入的地址，不执行互联网搜索。
_Avoid_: 订阅托管、订阅同步、搜索引擎、代理下载

**本机发现**：
从 Clash Verge Rev 已导入的本机配置中识别目标主订阅、子订阅和可选代理组的辅助过程。
_Avoid_: 上传配置、同步订阅、手工导入

**配置位置识别**：
按 Clash Verge Rev 官方规则检查有限的系统与便携配置位置；找不到时只检查用户明确指定的位置。
_Avoid_: 全盘搜索、任意文件扫描、自动迁移配置

**未验证订阅**：
未完成订阅检查或订阅检查失败的子订阅；用户补齐生成所需信息后仍可继续。
_Avoid_: 无效订阅、禁用订阅

**脚本写入**：
用户明确确认后，把有效嵌套脚本写入目标主订阅已经绑定的独立扩展脚本文件；它不等于 Clash Verge Rev 已重新加载或应用脚本。
_Avoid_: 自动应用、部署、接管客户端

**交付完成**：
嵌套脚本已经成功写入或成功复制；CLI 只允许用户重新配置或完成退出。写入完成无需更新主订阅，但仍需用户在 Clash Verge Rev 重新激活当前订阅，或激活所选的其他目标订阅。
_Avoid_: 已自动生效、已更新订阅

**管理会话**：
当前 CLI 进程中的配置草稿和与它同步生成的最新结果；进程退出后不保留。
_Avoid_: 页面会话、云端历史、永久备份

**生成后操作菜单**：
选择子订阅并生成结果后显示的单层数字菜单；“检查方向并继续”、追加、改名、详情、遮罩脚本和重新配置都直接列在同一层，不再通过“更多操作”进入第二层。每个数字菜单使用空行和文本分隔线建立层级，不依赖终端颜色表达含义。
_Avoid_: “更多操作”子菜单、只靠颜色区分层级

**产品边界**：
Clash Verge Kit 负责本机发现、订阅检查、嵌套脚本生成、遮罩预览、剪贴板导出和安全脚本写入；Clash Verge Rev 与 Mihomo 仍负责重新加载、应用脚本、选择当前代理和下载代理来源。它不提供浏览器界面、localhost 服务、公网部署、代理流量转发、订阅托管、云端同步、账号系统或替代 Clash Verge Rev 的客户端能力。
