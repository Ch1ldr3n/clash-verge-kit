# Security and privacy

不要在公开报告、Issue、截图或日志中包含真实订阅 URL、provider token、`profiles.yaml`、YAML 原文、完整生成脚本、cookie 或访问令牌。漏洞报告应使用仍能复现问题的最小脱敏数据。

## 数据生命周期

- 语言、订阅来源、检查摘要、生成规格和脚本只存在于当前 CLI 进程内存。
- 手工 URL 在用户输入时可见，随后不得在摘要、诊断或日志中再次显示。
- 终端只显示遮罩预览，不打印完整脚本。
- 只有用户明确确认方向后，完整脚本才能进入系统剪贴板或安全写入目标脚本文件。
- 剪贴板内容由操作系统和用户管理；工具不创建剪贴板临时文件。

## 本机目录

本机发现优先检查 Clash Verge Rev 的官方系统数据目录；Windows 还可从运行进程和安装记录定位便携配置。自动识别不递归扫描硬盘。未找到时，CLI 只检查用户明确输入的安装目录、配置目录或 `profiles.yaml` 候选，并只在当前进程中记住该位置。

目录解析会在当前进程内存中读取来源，以完成去重和身份匹配；初始目录只向终端返回名称、状态、是否为当前订阅和进程内随机 ID。完整来源只有在用户选择对应 ID 后才进入后续工作流，目录读取本身不会发起远程订阅请求。

## 远程订阅检查

远程读取只能由用户明确确认触发。它只接受不含 URL 凭据的公网 HTTPS 地址，拒绝 loopback、私网、链路本地和保留地址；每次跳转都会重新校验并固定到已验证地址。读取最多三次跳转；整个远程检查共享十秒绝对总时限，覆盖 DNS、跳转、直接或代理连接、TLS 和正文读取，连接同时保留无活动超时；解压后上限为 5 MiB，并限制响应格式和大小。

检查结果只保留名称候选、格式、`select` 组、节点数量和警告，不向终端输出节点、连接信息、规则或 YAML 原文。

- 本机、远程和手工名称在进入终端工作流前统一限制长度，并拒绝控制字符、ANSI/OSC 序列所需字符和双向文本控制字符；不安全名称不得进入菜单、确认或剪贴板流程。

## 脚本写入

写入目标必须位于已识别配置根目录下，且 `profiles` 目录、目标文件和真实路径不能通过符号链接或 Windows Junction 越出该根目录。目标必须是订阅已经绑定的官方 `s*.js` 文件。

只允许覆盖空白、官方默认 no-op 脚本或完整性校验通过的 Kit 已管理脚本。异常路径、特殊文件、缺失绑定、未知自定义脚本、无效脚本和并发变化都会被拒绝。CLI 不修改 `profiles.yaml`，不创建备份，也不重启或控制 Clash Verge Rev。

## 管理元数据

生成器继续产生有界、版本化且带 SHA-256 完整性值的管理元数据。解析器只读取元数据，不执行导入的 JavaScript；写入前还会单独验证脚本语法。CLI-only V1 不提供用户脚本导入、更新或复原交互，但保留这些核心格式语义以维持兼容。

## 进程与网络边界

CLI 是前台本机进程，不监听端口、不提供 HTTP API、不启动浏览器或后台服务。网络访问只有用户明确确认后的出站订阅检查，以及系统可能配置的本机 Mihomo 代理连接。

文件写入成功不代表运行配置已经应用。用户仍需回 Clash Verge Rev 重新激活目标订阅并验证；真实重新激活行为不由自动化测试代替。
